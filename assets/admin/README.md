Console-only assets.

Empty for now: the admin console reuses the shared VIPs logo from
assets/images/. Declared in pubspec so anything console-specific added later
(an ops icon set, an export template) ships without another pubspec change.
